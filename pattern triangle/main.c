#include <stdio.h>
#include <stdlib.h>

int main()
{
     int a,b=1,n;
    printf("enter the number of rows of triangle:");
    scanf("%d",&n);
    for(a=1;a<=b;b++){
        for(a=1;a<=b;a++){
            printf("%6d",a);
    }
   printf("\n");
  if(b>=n){break;}
}
}
// = means assigning a value == means equals
